[package]
name = "lumina-ai-chat"
version = "1.0.0"
description = "Lumina AI Chat"
authors = ["Lumina"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2" }

[dependencies]
tauri = { version = "2", features = ["devtools"] }

[features]
default = []
